# RP-01 Reality Blocker｜2026-09-15

Status: `NOT_OBSERVED`

## Verified environment

- Host: `MacBook-Pro-M4.local`
- Android Platform Tools: installed
- ADB: `37.0.1`
- `adb devices -l`: no device rows
- macOS USB scan: no Android / ONYX / BOOX / Dedao / MTP / Qualcomm match
- Existing USB enumeration shows only Realtek hub/HID devices

## Interpretation

The system has not reached Android RSA authorization yet. This is not an `unauthorized` ADB state; the reader is not currently visible as an Android USB data/debug device.

## Human gate

On the reader:

1. unlock and keep screen awake;
2. enable USB debugging / Android debugging;
3. if present, switch USB mode from charge-only to data/file-transfer mode;
4. reconnect a known data-capable USB cable/port;
5. approve the RSA fingerprint prompt if it appears.

For commonly documented Dedao reader firmware, the USB-debugging option is under a path similar to `设置 → 通用设置 → 应用设置 → USB调试模式`; wording can vary by model/firmware.

## Pass condition

`adb devices -l` must show exactly one intended reader. `unauthorized` requires human approval; `device` allows RP-01 identity collection.

No root, OEM unlock, firmware flash, or DRM modification is required or authorized.
